# Copilot CLI — raw dump 2026-05-21

**Routing role:** Framework-specific code patterns for judge-as-a-service
**Tool:** BLOCKED — Copilot CLI weekly rate limit / unavailable in sandbox
**Brief:** Episode 1. See ../episodes/01-spinoff-vs-core.md.

---

## Stub — engineering patterns relevant to both forks

1. **Rubric-as-config:** Store scoring dimensions in JSON Schema per tenant; version rubrics (`v1-airdrop`, `v1-martech`) — maps to existing `ScoringRubric` model in Django.

2. **Async judge queue:** POST `/score` → task id → poll/WebSocket — already matches Celery pattern; MarTech needs SLA <30s for copy paste.

3. **Precompute + cache:** Content-hash cache is the cost moat; marketing creatives need image hash + text hash.

4. **Human-in-the-loop appeals:** Farming verdicts need appeal flow before on-chain mint — reduces backlash risk (contrarian Grok stub).

5. **Separate prompt packs:** Do not fork repos; fork prompt modules (`prompts/reputation.py`, `prompts/performance_marketing.py`).

**Revisit:** Re-run `copilot -p '...' --deny-tool=shell --deny-tool=write` when rate limit resets.
